# Get employees with salary > 50000.
SELECT * 
FROM EMP
WHERE SALARY > 50000;

# Get employees from CSE department.
SELECT *
FROM EMP
WHERE DEPT = 'CSE' ;

# Sort employees by salary in descending order.
SELECT * 
FROM EMP
ORDER BY SALARY DESC;

# Count total employees.
SELECT COUNT(*)
FROM EMP;
